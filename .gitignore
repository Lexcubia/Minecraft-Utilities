# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/

# Node / pnpm
node_modules/
.pnpm-store/

# 仓库根构建产物（Vite `build/web`、Cargo `build/cargo-target`、免安装 `build/desktop`）；见 `scripts/build-artifacts.mjs`
/build/

# Tauri / Rust（未使用 `src-tauri/.cargo/config.toml` 时 Cargo 默认 target；保留忽略以免误提交）
src-tauri/target/
# Tauri 增量更新私钥（勿提交；CI 使用 GitHub Secret `TAURI_SIGNING_PRIVATE_KEY`）
src-tauri/tauri-updater.key

# IDE
.idea/
*.code-workspace
.history/

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Vite 本地
*.local

# Logs
*.log

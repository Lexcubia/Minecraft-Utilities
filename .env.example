# 复制为 .env 或 .env.local 后按需填写（勿将含密钥的 .env 提交到 Git）
# Vite 仅暴露以 VITE_ 开头的变量到前端代码：import.meta.env.VITE_*

# 应用标题（可选，未设置则沿用界面内文案）
# VITE_APP_TITLE=Minecraft Utilities

# 未来对接本机 Python 引擎 HTTP 时的基址（开发阶段可留空）
# VITE_ENGINE_API_BASE=http://127.0.0.1:8787

# 是否在 UI 中显示额外调试信息（true / false）
# VITE_DEBUG_UI=false

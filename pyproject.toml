[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minecraft-utilities"
version = "0.2.0"
description = "Minecraft Utilities: Python engine CLI (Tauri desktop companion)"
authors = [{ name = "Lexcubia" }]
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "httpx>=0.27,<0.29",
    "pydantic>=2.6,<3",
    "typer>=0.12,<0.16",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
    "ruff>=0.6,<1",
]

[project.scripts]
minecraft-utilities = "modpack_updater.cli:app"
modpack-updater = "modpack_updater.cli:app"

[tool.hatch.build.targets.wheel]
packages = ["python/modpack_updater"]

[tool.ruff]
line-length = 100
src = ["python", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["python"]
